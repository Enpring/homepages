<!DOCTYPE html>
<html lang="ko">
<head>
    <!-- head.ejs 파셜을 포함합니다. -->
    <%- include('../partials/head', { title: page.title }) %>
</head>
<body class="<%= theme.bodyClass %> min-h-screen flex flex-col font-sans antialiased">

    <!-- 1. 헤더 -->
    <%- include('../partials/header', { theme: theme, page: page }) %>

    <div class="flex-grow flex flex-col justify-center items-center">
        <main class="flex flex-col items-center justify-center text-center px-4 pt-20 pb-10">

            <!-- 환영 메시지 -->
            <h1 class="text-5xl md:text-7xl font-extrabold mb-4">
                <span class="bg-clip-text text-transparent bg-gradient-to-r <%= theme.headingGradient %>">
                    <%= page.title %>
                </span>
            </h1>

            <!-- 소개글 -->
            <p class="text-lg md:text-xl max-w-xl mb-8 <%= theme.subtextClass %>">
                <%= page.description %>
            </p>

        </main>

        <!-- 3. 푸터 (폼 포함) -->
        <%- include('../partials/footer', { theme: theme, page: page }) %>

    </div> <!-- [수정됨] 래퍼 <div> 종료 -->

    <!-- 4. 스크립트 -->
    <%- include('../partials/scripts') %>
</body>
</html>
